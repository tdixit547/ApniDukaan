package coldblooded.project.prototype.service;

import org.springframework.stereotype.Service;

@Service
public class ImageService {
    // Placeholder service for future image upload/management functionality.
    // Can be extended to handle file uploads to cloud storage (e.g., AWS S3, Cloudinary).
}
